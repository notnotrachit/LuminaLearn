# Environment Variables Template for LuminaLearn
# Copy this file to .env and configure your production email settings

# Email Backend Configuration
# For production, use: django.core.mail.backends.smtp.EmailBackend
# For development/testing, use: django.core.mail.backends.console.EmailBackend
EMAIL_BACKEND=django.core.mail.backends.console.EmailBackend

# SMTP Email Settings (for production)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password

# Default sender email address
DEFAULT_FROM_EMAIL=noreply@luminallearn.com

# Example Gmail Settings:
# 1. Enable 2-Factor Authentication for your Gmail account
# 2. Generate an App Password: https://support.google.com/accounts/answer/185833
# 3. Use the App Password as EMAIL_HOST_PASSWORD (not your regular password)
# 4. Set EMAIL_HOST_USER to your Gmail address

# Example for other email providers:
# 
# Outlook/Hotmail:
# EMAIL_HOST=smtp-mail.outlook.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# 
# Yahoo:
# EMAIL_HOST=smtp.mail.yahoo.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# 
# Custom SMTP:
# EMAIL_HOST=mail.yourcompany.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True

# Security Notes:
# - Never commit actual credentials to version control
# - Use environment variables or a secure .env file
# - Consider using email services like SendGrid, Mailgun, or AWS SES for production